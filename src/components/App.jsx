import React from 'react';

import { SearchInput } from './searchInput';
import { Layout } from './layout';

export const App = () => {
  return (
    <Layout>
      <SearchInput />
    </Layout>
  );
};
